# Core dependencies for quantitative trading system
# yfinance provides free access to Yahoo Finance data
yfinance>=0.2.28
pandas>=2.0.0
numpy>=1.24.0

# Optional: for better data visualization
matplotlib>=3.7.0
plotly>=5.17.0

# Web UI framework
streamlit>=1.28.0

# Database (SQLite is included with Python, but we list it for clarity)
# For production, consider upgrading to PostgreSQL with psycopg2

# Utilities and performance
# Note: concurrent.futures is part of Python standard library (no install needed)

# Email functionality (for future alerts)
# sendgrid>=1.9.0  # Uncomment when implementing email alerts
# OR
# smtplib is part of Python standard library

# AI/ML capabilities (for future expansion)
# Note: Currently using rule-based AI. Uncomment below for LLM integration:
# openai>=1.0.0  # For OpenAI API integration
# anthropic>=0.3.0  # For Claude API integration

